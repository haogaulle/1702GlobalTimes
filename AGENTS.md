# 环球时报电子化：文稿落地规范

本仓库是静态单页文章集，通过根目录 `index.html` 汇总并由 GitHub Pages 发布。新增或改造文稿时，以现有同类型成品为模板；不要另起一套视觉语言，也不要把现有内联样式抽成公共 CSS，除非任务明确要求全站重构。

## 先判断稿型

| 稿型 | 首选参考页 | 视觉识别 | 内容骨架 |
| --- | --- | --- | --- |
| 人物（人物专访） | `恒特勒：荣耀的背后刻着一道孤独.html`（模块最全）；简洁版可参考 `川吉尔，一代政坛硬汉.html` | 蓝色 `#07589b`；页眉“环球时报 / 人物”；口号“观察世界　记录时代人物” | 栏目标、主标题、副标题、来源/记者/地点/日期、导语、引语框、问答实录、后记、落款，可选附件与评论 |
| 时事锐评（现有页面也写作“时政锐评”） | `又一场慕尼黑?.html`；无导语时参考 `英国内政危机，川吉尔能否力挽澜.html` | 红色 `#a6191e`；页眉“环球时评 / 锐评”；口号“观察世界　传递法国声音” | 栏目标、标题、可选副标题、来源/记者/日期、可选导语、连续评论正文、署名或编者注、评论 |
| 高峰论坛（政府博客） | `挪威政府关于政权托管安排的公函.html`；无评论短帖参考 `法兰西独立号外.html` | 海军蓝 `#17365d`，红色标题强调；页眉“环球时报 / 高峰论坛”；口号“汇集各国政府声音　直面全球议题” | 账号头像/旗帜、账号名与认证标记、账号说明、帖文标题、正文、发布时间、帖子标签/统计，可选评论与嵌套回复 |
| 环球调研 | `关于法兰西地区独立的支持率.html` | 青绿 `#176b68`，棕金 `#b36a2e` 作调查标签；页眉“环球时报 / 环球调研”；口号“深入全球现场　洞察时代脉动” | 栏目标、标题、副标题、记者/地点/栏目、调查导语、统计摘要、受访者逐条观点，可选公告/来源说明 |
| 特别专栏 | `环球时报特别专栏.html` | 紫色 `#6b3f8c`，浅紫 `#9b6bb5` 作标签；页眉“环球时报 / 特别专栏”；口号“聚焦时代变局　聆听世界回响” | 栏目标、标题、来源信息、事件导语、各方回应；可选认证标记 |
| 体育 | `足球联赛报告.html` | 纯橙色 `#c65318`；页眉“环球时报 / 体育”；口号“记录赛场风云　见证青春力量” | 栏目标、主标题、副标题、发布方/地点/日期、导语、数据摘要、排行榜、球队数据、图表、阶段详报与落款 |

“人物”是栏目名，当前成品的页面标签统一为“人物专访”。“时事锐评”和“时政锐评”在历史页面中并存；新稿优先采用用户原稿中的栏目写法，用户未指定时用“时事锐评”，并确保页面 `<title>`、栏目标签和首页分类一致。

## 全站不变的 HTML 与样式约定

- 每篇文章是一个可独立打开的完整 HTML5 文件：`<!doctype html>`、`<html lang="zh-CN">`、UTF-8、viewport、准确的 description 和 `<title>` 都不可缺少。
- 使用语义骨架：顶部装饰线 → `.masthead` → `<main>` → `<article>`；独立内容块使用 `<section>`，补充性公告或引语使用 `<aside>`，图片及说明使用 `<figure>/<figcaption>`。
- `aria-labelledby` 必须指向页面内真实且唯一的 `id`；纯装饰图形不冒充正文图片。旗帜图案如用 CSS 绘制，保留 `role="img"` 和准确的 `aria-label`。
- 桌面页眉内容宽度为 `min(1120px, calc(100% - 40px))`，高度 94px；文章卡片距顶部 26px，底部 56px。常规文章正文最大宽度 830px；调研页可放宽到 880px；论坛帖主容器为 830px。
- 页面底色是浅灰，正文纸张为白色并带轻微阴影。正文中文优先使用宋体/衬线字体栈：`"Noto Serif SC", STSong, SimSun, serif`；界面、元数据和标签使用无衬线字体栈：`"Noto Sans SC", "Microsoft YaHei", "PingFang SC", sans-serif`。
- 桌面正文通常为 17–19px、行高 1.9–2、两端对齐。新闻评论正文段落缩进 `2em`；采访问答、帖子、导语和评论不缩进。
- 所有页面必须有 `@media (max-width: 680px)`：页眉降至 74px并隐藏口号；主卡片满宽、去阴影；正文左右留白约 18–20px；正文降至 16–17px；不可产生横向滚动。
- 样式变量写在 `:root`，优先沿用同稿型参考页的变量名、色值、字号和间距。只为内容确实需要的新模块增加样式，不随意更换栏目主色。
- 页面保持纯静态、无构建依赖、无远程字体或脚本依赖。图片优先使用仓库内相对路径；确有单文件交付需要时可以使用 data URI，但要填写 `alt`，并留意它会显著增大文件。
- 不擅自润色、纠错或改写原稿中的专名、语气、符号、评论顺序和历史写法。无法辨认的字用带说明的标记保留，不做无依据猜测。

## 各稿型的模块细则

### 人物专访

使用 `.section-label`、`h1`、可选 `.subtitle`、`.meta`。有场景交代时放入 `.lead`；从原文提炼一句原话放入 `.pullquote`，不能杜撰。问答区用 `.interview` 包裹，每组使用 `.exchange`，问题和回答分别为 `.question` / `.answer`，首段以 `.speaker` 标明“问”“答”。同一回答的续段仍用 `.answer`，但不重复“答”徽标。

列表回答用 `.response-list`。采访后的记者观察使用 `.epilogue`，结稿信息使用 `.dateline`。附件使用 `.attachments` 和 `.attachment-card`；评论使用 `.comments`。没有原始素材的模块直接省略，不放空标题或占位文字。

### 时事锐评

正文放在 `.body` 中，每个自然段独立为 `<p>`。开篇提要存在时使用 `.lead`；副题存在时沿用人物页的 `.subtitle` 结构。记者署名可用 `.byline`，编辑说明用 `.editor-note`，二者以参考页现有写法为准。

锐评重心是连续论述，不应改造成问答卡片。品牌必须写“环球时评”，主色使用红色。评论区结构与人物页相同，但顶部分隔线跟随红色主题。

### 高峰论坛

它模拟经过认证的政府/组织博客，不使用普通新闻页的栏目标签 + 大标题结构。`.post-head` 承载 `.avatar`、`.account-name`、`.verified` 和 `.account-meta`；`.post-body` 承载 `.post-title`、`.post-text`、`.post-time`；底部使用 `.post-meta` 或 `.post-stats`。

国旗可用 `.flag` 的 CSS 渐变绘制，但必须按发布账号调整颜色和 `aria-label`。帖子有互动时加入 `.comments`；回复放在所属 `.comment` 内的 `.reply`，不要提升为并列评论。评论数、页面显示的统计数和实际顶级 `.comment` 数应一致。

### 环球调研

调研结果必须由原始观点计算，不根据倾向猜测。摘要用 `.poll-summary`，每个选项用 `.poll-row`；票数、总人数和百分比要互相校验。条形宽度按 `票数 / 总数 × 100%` 设置，并用 `data-choice="support|neutral|oppose"` 区分颜色；零票写 `width:0`。

受访观点使用 `.comment`，姓名与立场放在 `.comment-author` 内，立场/国家信息使用 `.country`。P.S. 可用 `.ps`。后续公告属于补充内容时用 `.announcement`，不要与调查样本混算。

### 特别专栏

特别专栏用于围绕同一突发事件汇集多方即时回应，不加入投票统计，也不改造成采访问答。使用紫色 `#6b3f8c` 为栏目主色、浅紫 `#9b6bb5` 为标签色，页眉口号固定为“聚焦时代变局　聆听世界回响”。事件背景放入 `.lead`，各方回应由 `.responses` 包裹，每位发言者使用独立的 `.response`，姓名作为对应区块的标题；原稿明确标注认证账号时才显示 `.verified`。

### 体育

体育稿用于联赛报告、赛况汇总、排行榜和足协公告。使用纯橙色 `#c65318` 为栏目主色，顶部装饰线、报头、栏目标签、章节分隔线和数据强调统一使用该主色及其透明度或明度变化，不使用渐变，也不混入其他稿型的蓝、红、海军蓝、青绿或紫色主题。页眉固定为“环球时报 / 体育”，口号固定为“记录赛场风云　见证青春力量”。

阶段摘要使用 `.scoreboard` 和 `.stat`；排行榜优先使用语义化 `<table>`；球队对比使用 `.team-grid` 和 `.team-card`；阶段详报使用 `.details`。原稿包含走势、势力分布或比赛数据图时，使用 `<figure>`、可缩放的内联 SVG 和 `<figcaption>`，并为 SVG 提供 `<title>`、`<desc>` 及正确的 `aria-labelledby`。图表内用于区分球队或数据序列的颜色不视为栏目主题色，但页面框架与文字强调仍须保持橙色单色体系。月份、轮次、比分等轴标签必须忠实于原稿，不凭版式推断。

## 新增一篇文稿的工作流

1. 阅读对应 `.txt` 原稿（如有），识别稿型、标题层级、作者/记者、地点、日期、评论、回复、附件和无法辨认处。
2. 复制上表中同稿型且模块最接近的 HTML；保持其结构与 CSS，只删除此次不需要的可选模块。
3. 替换 metadata 与正文。日期已知时使用 `<time datetime="YYYY-MM-DD">显示文本</time>`；年份不确定时不要猜造机器日期，可只保留普通文本。
4. 如有评论，保持原稿顺序和嵌套关系，并核对页面显示的评论数。评论数默认统计顶级评论，不把 `.reply` 另算一条，除非原稿明确采用别的口径。
5. 在根目录 `index.html` 的 `.articles` 中新增一张卡片，分类名称与文章页一致，标题准确。链接必须指向实际文件；中文和特殊标点可以使用正确的相对 URL 编码，尤其留意 `?`、引号和空格。
6. 有纯文本来源时保留同名 `.txt` 作为档案；HTML 文件名原则上与稿件标题一致。不要重命名既有文件，以免 GitHub Pages 外链失效。

## 完成前检查

- 在浏览器中分别以桌面宽度和不超过 680px 的窄屏检查文章页：首页可进入、没有横向滚动、长标题不溢出、评论回复层级清楚、图片不过宽。
- 点击首页新卡片并回查文件名；包含 `?`、弯引号或其他特殊字符的链接尤其要实际打开验证。
- 核对 `<title>`、description、页面 `h1`、栏目标签、记者/来源、地点与日期；`datetime` 使用合法 ISO 日期。
- 核对 HTML 标签闭合、所有 `id` 唯一、所有 `aria-labelledby` 有对应目标、所有信息图片有 `alt`。
- 核对调研票数/百分比/总数，或帖子及评论区显示数量与实际条目数。
- 查看 `git diff`，确认没有改动无关文章、没有把 `.DS_Store` 等系统文件纳入提交，也没有因格式化而重写大段 data URI。

除非任务明确要求，不要批量统一旧页面的“时事/时政”命名、日期口径或格式差异；本规范主要约束新增内容，并以最小改动维护既有页面。
